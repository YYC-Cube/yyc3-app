# 0379.world 域名访问流程

## **中文流程图**，直观展示备案域名如何通过阿里云服务器中转，再进入 Vercel 项目

---

## 🌐 域名访问流程图

```plaintext
用户浏览器
     │
     ▼
访问备案域名 (0379.world 或 yyc.0379.world)
     │
     ▼
阿里云 DNS 解析 → 指向公网 IP (8.152.195.33)
     │
     ▼
阿里云服务器 (yyc3-33)
 ┌───────────────────────────────┐
 │ Nginx/Apache 配置              │
 │ - 主域名 → 仪表盘项目          │
 │ - 子域名 → proxy_pass 转发     │
 └───────────────────────────────┘
     │
     ▼
Vercel 部署项目 (如 yyc3.vercel.app / xy.yanyu.red)
     │
     ▼
返回页面内容 → 用户浏览器展示
```

---

## 🔑 关键点说明

1. **备案域名**：必须解析到国内服务器 IP，保证合规。  
2. **阿里云服务器**：作为中转站，负责反向代理或重定向。  
3. **Nginx 配置**：通过 `proxy_pass` 将不同子域名请求转发到对应的 Vercel 项目。  
4. **仪表盘项目**：主域名 `0379.world` 用来汇集和展示所有子项目入口。  

---

这样，你的整体架构就形成了一个 **合法备案 + 国内中转 + 国外部署** 的完整闭环。  

### 仪表盘项目概览

你提供的仓库是一个企业级 AI 智能管理平台（Next.js 16 + React 19 + TypeScript + Tailwind），内置数据分析、权限管理、通知中心、移动端适配等模块，适合作为“集成中心服务器”的门户与汇集入口。示例运行页面展示了“YYC³ AI 智能外呼系统”的看板、搜索与通知逻辑，证明该仪表盘能承载应用列表与状态概览。

---

### 作为集成中心的推荐架构

- **主域名门户**：将 0379.world 指向你的阿里云服务器，部署该仪表盘项目作为统一入口与导航。
- **子域名代理**：为 24 个项目配置子域名，并在阿里云服务器用 Nginx 反向代理到各自的 Vercel 项目 URL（或已有自定义域）。
- **应用清单页面**：在仪表盘主页或专用路由中，以网格卡片展示 24 个项目的名称、简介、状态和入口链接；支持移动端组件实现良好适配。

---

### 仪表盘项目的快速接入步骤

1. **拉取与安装**
   - 克隆仓库并安装依赖，使用 pnpm/yarn/npm 均可；本项目提供了开发与构建命令说明。
2. **环境变量配置**
   - 在 .env.local 中设置你需要的后端地址或静态清单；仓库提供 .env.example 作为参考。
3. **页面与路由**
   - 在 app/ 下创建“应用汇总”页面（如 app/apps/page.tsx），并使用 components 与 hooks 组合展示清单和状态。
4. **权限与可见性（可选）**
   - 若需按角色显示项目入口，使用 auth 模块的 PermissionGate 进行条件渲染。
5. **部署到阿里云服务器**
   - 使用 Node 运行或容器化；前面以 Nginx 做反向代理，暴露 80/443。

以上步骤与目录结构、技术栈、组件库均与仓库文档一致。

---

### 示例：应用汇总页（Next.js）

```tsx
// app/apps/page.tsx
import Link from "next/link";

const projects = [
  { name: "YYC³ AI 智能外呼", url: "https://yyc3.vercel.app", desc: "AI驱动的外呼与审阅闭环" },
  { name: "星云操作系统", url: "https://xy.yanyu.red", desc: "多租户与权限管理" },
  // ...继续补充到 24 项
];

export default function AppsPage() {
  return (
    <main className="p-8 space-y-6">
      <h1 className="text-2xl font-bold">应用汇总（24 项）</h1>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {projects.map(p => (
          <div key={p.name} className="border rounded p-4">
            <h2 className="text-lg font-semibold">{p.name}</h2>
            <p className="text-sm text-muted-foreground">{p.desc}</p>
            <Link className="text-primary mt-2 inline-block" href={p.url} target="_blank">进入应用 →</Link>
          </div>
        ))}
      </div>
    </main>
  );
}
```

- 可根据仓库中的移动端组件（如 MobileNav、MobileStatsGrid）添加移动端适配。
- 若需要状态概览卡片，可参考仪表盘和图表组件目录进行复用。

---

### 示例：Nginx 代理 24 个项目

```nginx
# 主域名：门户（仪表盘）
server {
    listen 80;
    server_name 0379.world;
    location / {
        proxy_pass http://localhost:3000; # 仪表盘项目运行端口
    }
}

# 子域名：逐个代理到 Vercel 或自定义域
server {
    listen 80;
    server_name yyc.0379.world;
    location / { proxy_pass https://yyc3.vercel.app; }
}

server {
    listen 80;
    server_name xy.0379.world;
    location / { proxy_pass https://xy.yanyu.red; }
}

# …按此模式扩展到 24 项
```

- 你也可以将这些目标 URL 抽象为变量，便于统一维护与更新。
- 结合项目中“通讯中心”“系统控制台”等页面，可在门户侧展示运行状态与通知.

---

### DNS 与解析清单（执行顺序）

- 将 0379.world 的 A 记录解析到阿里云服务器公网 IP。
- 为每个子域名（24 个）添加 A 记录解析到同一公网 IP。
- 在服务器完成上述 Nginx 代理配置，并启动仪表盘项目服务。

这样即可实现“备案域名 → 阿里云服务器 → 门户仪表盘 + 24 子域名代理到各自项目”的合规中转与集中展示。

---

### 可选增强

- 使用权限模块按角色显示项目入口（super_admin/admin/manager 等），参考 auth 目录与 PermissionGate。
- 加入通知中心与活动流组件，展示部署、健康与审阅队列变更。
- 将“应用清单”改为动态来源（后端 API 或配置文件），便于批量增删项目.

## **“24 项应用清单”** 的 JSON 配置与前端适配代码，完全基于你仓库的 Next.js + React + Tailwind 架构。这样你可以直接在仪表盘项目中加载并展示所有应用入口

---

## 📑 JSON 配置示例（apps.json）

```json
[
  {
    "name": "YYC³ AI 智能外呼",
    "url": "https://yyc3.vercel.app",
    "desc": "AI驱动的外呼与审阅闭环"
  },
  {
    "name": "星云操作系统",
    "url": "https://xy.yanyu.red",
    "desc": "企业级权限管理与数据分析"
  },
  {
    "name": "未来仪表盘",
    "url": "https://dashboard.0379.world",
    "desc": "统一门户与应用汇总"
  },
  {
    "name": "项目4",
    "url": "https://project4.vercel.app",
    "desc": "示例应用4"
  },
  {
    "name": "项目5",
    "url": "https://project5.vercel.app",
    "desc": "示例应用5"
  },
  {
    "name": "项目6",
    "url": "https://project6.vercel.app",
    "desc": "示例应用6"
  },
  {
    "name": "项目7",
    "url": "https://project7.vercel.app",
    "desc": "示例应用7"
  },
  {
    "name": "项目8",
    "url": "https://project8.vercel.app",
    "desc": "示例应用8"
  },
  {
    "name": "项目9",
    "url": "https://project9.vercel.app",
    "desc": "示例应用9"
  },
  {
    "name": "项目10",
    "url": "https://project10.vercel.app",
    "desc": "示例应用10"
  },
  {
    "name": "项目11",
    "url": "https://project11.vercel.app",
    "desc": "示例应用11"
  },
  {
    "name": "项目12",
    "url": "https://project12.vercel.app",
    "desc": "示例应用12"
  },
  {
    "name": "项目13",
    "url": "https://project13.vercel.app",
    "desc": "示例应用13"
  },
  {
    "name": "项目14",
    "url": "https://project14.vercel.app",
    "desc": "示例应用14"
  },
  {
    "name": "项目15",
    "url": "https://project15.vercel.app",
    "desc": "示例应用15"
  },
  {
    "name": "项目16",
    "url": "https://project16.vercel.app",
    "desc": "示例应用16"
  },
  {
    "name": "项目17",
    "url": "https://project17.vercel.app",
    "desc": "示例应用17"
  },
  {
    "name": "项目18",
    "url": "https://project18.vercel.app",
    "desc": "示例应用18"
  },
  {
    "name": "项目19",
    "url": "https://project19.vercel.app",
    "desc": "示例应用19"
  },
  {
    "name": "项目20",
    "url": "https://project20.vercel.app",
    "desc": "示例应用20"
  },
  {
    "name": "项目21",
    "url": "https://project21.vercel.app",
    "desc": "示例应用21"
  },
  {
    "name": "项目22",
    "url": "https://project22.vercel.app",
    "desc": "示例应用22"
  },
  {
    "name": "项目23",
    "url": "https://project23.vercel.app",
    "desc": "示例应用23"
  },
  {
    "name": "项目24",
    "url": "https://project24.vercel.app",
    "desc": "示例应用24"
  }
]
```

> 你可以把这个文件放在 `public/apps.json` 或 `lib/apps.json` 中，方便前端读取。

---

## 🖥️ 前端适配代码（Next.js 页面）

```tsx
// app/apps/page.tsx
import { useEffect, useState } from "react";
import Link from "next/link";

interface Project {
  name: string;
  url: string;
  desc: string;
}

export default function AppsPage() {
  const [projects, setProjects] = useState<Project[]>([]);

  useEffect(() => {
    fetch("/apps.json")
      .then(res => res.json())
      .then(data => setProjects(data));
  }, []);

  return (
    <main className="p-8 space-y-6">
      <h1 className="text-2xl font-bold">应用汇总（24 项）</h1>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {projects.map(p => (
          <div key={p.name} className="border rounded p-4 shadow-sm hover:shadow-md">
            <h2 className="text-lg font-semibold">{p.name}</h2>
            <p className="text-sm text-gray-600">{p.desc}</p>
            <Link href={p.url} target="_blank" className="text-blue-600 mt-2 inline-block">
              进入应用 →
            </Link>
          </div>
        ))}
      </div>
    </main>
  );
}
```

---

## ✨ 特点

- **JSON 配置化**：你只需维护 `apps.json` 文件即可，前端自动加载。
- **响应式布局**：使用 Tailwind 的 `grid`，在桌面端显示三列，在移动端显示一列。
- **可扩展性**：未来增加或删除项目，只需修改 JSON，不需要改代码。

---

## **权限控制示例**

当前的代码片段展示了 **PermissionGate** 组件，用于在前端根据用户权限控制内容显示。这说明你的项目是基于 **Next.js + React + TypeScript** 的前端应用，适合容器化部署。下面我为你提供一个 **Docker + Nginx 一键部署脚本**，用于快速在阿里云服务器上运行并代理你的应用。

---

## 🐳 Dockerfile（构建 Next.js 应用）

```dockerfile
# 使用 Node.js 构建阶段
FROM node:20-alpine AS builder

WORKDIR /app

# 安装依赖
COPY package.json package-lock.json* pnpm-lock.yaml* yarn.lock* ./
RUN npm install --frozen-lockfile

# 拷贝源代码
COPY . .

# 构建 Next.js 应用
RUN npm run build

# 使用轻量级镜像运行
FROM node:20-alpine AS runner

WORKDIR /app

ENV NODE_ENV production

# 拷贝构建产物
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/public ./public
COPY --from=builder /app/package.json ./package.json

# 安装生产依赖
RUN npm install --omit=dev --ignore-scripts

EXPOSE 3000

CMD ["npm", "run", "start"]
```

---

## 🌐 Nginx 配置（反向代理）

在阿里云服务器上，你可以用 Nginx 将备案域名指向 Docker 容器。

```nginx
server {
    listen 80;
    server_name 0379.world;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## ⚙️ docker-compose.yml（快速启动）

```yaml
version: "3.9"

services:
  dashboard:
    build: .
    container_name: yyc3-dashboard
    restart: always
    ports:
      - "3000:3000"
    networks:
      - webnet

  nginx:
    image: nginx:latest
    container_name: nginx-proxy
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
      - "443:443"
    networks:
      - webnet

networks:
  webnet:
```

---

## 🚀 一键部署流程

1. 将上述 `Dockerfile`、`docker-compose.yml` 和 `nginx.conf` 放在项目根目录。  
2. 在服务器执行：

   ```bash
   docker-compose up -d --build
   ```

3. 确保 DNS 已将 `0379.world` 指向服务器公网 IP。  
4. 浏览器访问 `http://0379.world` 即可看到仪表盘项目。  

---

## ✨ 配置特点

- **Dockerfile**：保证前端应用可移植、可复现。  
- **Nginx**：负责备案域名的反向代理，合规访问。  
- **docker-compose**：一键启动，自动拉起应用和代理。  

---

## 扩展脚本，支持 **多子域名代理到不同 Vercel 项目**（比如 24 个项目），这样你只需维护一个 `nginx.conf` 就能完成所有代理？

好的，Yu，我来帮你分两部分回答：  

---

## 🐳 一、扩展 Docker + Nginx 脚本，支持多子域名代理到不同 Vercel 项目

你要的场景是：备案域名和子域名都解析到阿里云服务器，然后由 Nginx 将不同子域名请求转发到对应的 Vercel 项目。下面是一个 **docker-compose + Nginx 配置模板**，支持 24 个子域名代理。

### docker-compose.yml

```yaml
version: "3.9"

services:
  nginx:
    image: nginx:latest
    container_name: nginx-proxy
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
      - "443:443"
    networks:
      - webnet

networks:
  webnet:
```

### nginx.conf（示例展示 3 个，按此模式扩展到 24 个）

```nginx
# 主域名门户站点
server {
    listen 80;
    server_name 0379.world;
    location / {
        proxy_pass http://127.0.0.1:3000; # 仪表盘项目容器
    }
}

# 子域名代理到 Vercel 项目
server {
    listen 80;
    server_name yyc.0379.world;
    location / {
        proxy_pass https://yyc3.vercel.app;
    }
}

server {
    listen 80;
    server_name xy.0379.world;
    location / {
        proxy_pass https://xy.yanyu.red;
    }
}

server {
    listen 80;
    server_name dash.0379.world;
    location / {
        proxy_pass https://dashboard.vercel.app;
    }
}

# ……依次类推，直到 project24.0379.world
```

> 需要在 `nginx.conf` 中为每个子域名写一个 `server` 块，指向对应的 Vercel 项目 URL。这样就能实现 24 个子域名代理。

---

## 📂 二、在当前仓库中寻找“行业分类”的路径文件

根据你打开的 **yyc3-futuristic-dashboard** 仓库结构：

- **业务逻辑与数据定义**：在 `lib/` 目录下  
  - `lib/ai-engine.ts` → AI 分析逻辑  
  - `lib/auth/` → 权限与角色  
  - `lib/chart-data-generator.ts` → 图表数据生成  
  - `lib/notifications/` → 通知系统  

- **页面路由与展示**：在 `app/` 目录下  
  - `app/analytics/` → 数据分析页面  
  - `app/data-center/` → 数据中心页面  
  - `app/network/` → 网络监控页面  
  - `app/security/` → 安全防护页面  
  - `app/console/` → 系统控制台页面  

👉 如果你要做 **行业分类（如金融、制造、零售等）**，最合适的路径是：

- 在 **`app/`** 下新建一个目录，例如 `app/industries/`，每个行业一个子页面。  
- 或者在 **`lib/`** 下新建一个 `industries.ts` 文件，定义行业分类的数据结构（枚举/字典），然后在前端页面调用。  

### 示例：lib/industries.ts

```ts
/**
 * 行业分类数据结构
 */
export const industries = [
  { id: "finance", name: "金融行业", desc: "银行、证券、保险等金融服务" },
  { id: "manufacturing", name: "制造业", desc: "工业生产、供应链管理" },
  { id: "retail", name: "零售业", desc: "电商、门店、客户管理" },
  { id: "healthcare", name: "医疗行业", desc: "医院、诊所、健康数据分析" },
  // …继续扩展
];
```

然后在 `app/industries/page.tsx` 中加载并展示这些分类。

---

## ✨ 总结

- **多子域名代理**：在 `nginx.conf` 中为每个子域名写一个 `server` 块，指向对应 Vercel 项目 URL。  
- **行业分类路径**：推荐在 `lib/industries.ts` 定义数据，在 `app/industries/` 下创建页面展示。  

---

## 三、把 **行业分类页面的 Next.js 前端代码模板**写出来？这样你可以直接在仪表盘项目中展示行业分类导航

## **24 个行业场景的名称与缩写对照表**，已映射函数进行语义审核与整理，方便在云服务器核对

---

## ✅ 24 个行业分类名称与缩写对照表

| 行业名称       | 缩写代码     | 图标 Emoji | 图标名称（iconName） |
|----------------|--------------|------------|------------------------|
| 智慧农业       | AGR          | 🌱         | Sprout                |
| 餐饮服务       | FB           | 🍽️         | Utensils              |
| 股票金融       | FN           | 📈         | TrendingUp            |
| 智慧城市       | GOV          | 🏢         | Building2             |
| 人力资源       | HR           | 👥         | Users                 |
| 医疗健康       | MED          | ❤️         | Heart                 |
| 媒体娱乐       | MEDIA        | 🎬         | Film                  |
| 智能制造       | MANU         | 🏭         | Factory               |
| 智能编程       | CORE         | 💻         | Code                  |
| 智能文创       | CULTURAL     | 🎨         | Palette               |
| 智能教育       | EDU          | 🎓         | GraduationCap         |
| 能源管理       | ENERGY       | ⚡         | Zap                   |
| 环境保护       | ENV          | 🍃         | Leaf                  |
| 法律服务       | LAW          | ⚖️         | Scale                 |
| 智慧物流       | LOG          | 🚚         | Truck                 |
| 实体经营       | ENT          | 💼         | Briefcase             |
| 地产建筑       | REAL         | 🏠         | Home                  |
| 智慧零售       | RETAIL       | 🛒         | ShoppingCart          |
| 智能交通       | TRAFFIC      | 🚗         | Car                   |
| 旅游酒店       | TOURISM      | ✈️         | Plane                 |
| 智慧养老       | ELDER        | 💓         | HeartPulse            |
| 技术集成       | API          | 🖥️         | Cpu                   |
| 数据中心       | DC           | 💾         | Database              |
| 金融科技       | FINANCE      | 💰         | DollarSign            |

---

## **行业分类 JSON 配置文件**，用于前端展示或后端调用

## 📁 文件路径建议  

建议保存为：`lib/config/industries.json`

---

## 📄 文件内容

```json
/**
 * @file industries.json
 * @description 24个行业分类配置，用于AI智能管理平台的行业场景选择与展示
 * @author YYC³
 * @version 1.0.0
 * @created 2025-10-15
 */

[
  {
    "id": "yyc3-agr",
    "name": "智慧农业",
    "code": "AGR",
    "icon": "Sprout",
    "path": "/industries/agr"
  },
  {
    "id": "yyc3-fb",
    "name": "餐饮服务",
    "code": "FB",
    "icon": "Utensils",
    "path": "/industries/fb"
  },
  {
    "id": "yyc3-fn",
    "name": "股票金融",
    "code": "FN",
    "icon": "TrendingUp",
    "path": "/industries/fn"
  },
  {
    "id": "yyc3-gov",
    "name": "智慧城市",
    "code": "GOV",
    "icon": "Building2",
    "path": "/industries/gov"
  },
  {
    "id": "yyc3-hr",
    "name": "人力资源",
    "code": "HR",
    "icon": "Users",
    "path": "/industries/hr"
  },
  {
    "id": "yyc3-med",
    "name": "医疗健康",
    "code": "MED",
    "icon": "Heart",
    "path": "/industries/med"
  },
  {
    "id": "yyc3-media",
    "name": "媒体娱乐",
    "code": "MEDIA",
    "icon": "Film",
    "path": "/industries/media"
  },
  {
    "id": "yyc3-manu",
    "name": "智能制造",
    "code": "MANU",
    "icon": "Factory",
    "path": "/industries/manu"
  },
  {
    "id": "yyc3-core",
    "name": "智能编程",
    "code": "CORE",
    "icon": "Code",
    "path": "/industries/core"
  },
  {
    "id": "yyc3-cultural",
    "name": "智能文创",
    "code": "CULTURAL",
    "icon": "Palette",
    "path": "/industries/cultural"
  },
  {
    "id": "yyc3-edu",
    "name": "智能教育",
    "code": "EDU",
    "icon": "GraduationCap",
    "path": "/industries/edu"
  },
  {
    "id": "yyc3-energy",
    "name": "能源管理",
    "code": "ENERGY",
    "icon": "Zap",
    "path": "/industries/energy"
  },
  {
    "id": "yyc3-env",
    "name": "环境保护",
    "code": "ENV",
    "icon": "Leaf",
    "path": "/industries/env"
  },
  {
    "id": "yyc3-law",
    "name": "法律服务",
    "code": "LAW",
    "icon": "Scale",
    "path": "/industries/law"
  },
  {
    "id": "yyc3-log",
    "name": "智慧物流",
    "code": "LOG",
    "icon": "Truck",
    "path": "/industries/log"
  },
  {
    "id": "yyc3-ent",
    "name": "实体经营",
    "code": "ENT",
    "icon": "Briefcase",
    "path": "/industries/ent"
  },
  {
    "id": "yyc3-real",
    "name": "地产建筑",
    "code": "REAL",
    "icon": "Home",
    "path": "/industries/real"
  },
  {
    "id": "yyc3-retail",
    "name": "智慧零售",
    "code": "RETAIL",
    "icon": "ShoppingCart",
    "path": "/industries/retail"
  },
  {
    "id": "yyc3-traffic",
    "name": "智能交通",
    "code": "TRAFFIC",
    "icon": "Car",
    "path": "/industries/traffic"
  },
  {
    "id": "yyc3-tourism",
    "name": "旅游酒店",
    "code": "TOURISM",
    "icon": "Plane",
    "path": "/industries/tourism"
  },
  {
    "id": "yyc3-elder",
    "name": "智慧养老",
    "code": "ELDER",
    "icon": "HeartPulse",
    "path": "/industries/elder"
  },
  {
    "id": "yyc3-api",
    "name": "技术集成",
    "code": "API",
    "icon": "Cpu",
    "path": "/industries/api"
  },
  {
    "id": "yyc3-dc",
    "name": "数据中心",
    "code": "DC",
    "icon": "Database",
    "path": "/industries/dc"
  },
  {
    "id": "yyc3-finance",
    "name": "金融科技",
    "code": "FINANCE",
    "icon": "DollarSign",
    "path": "/industries/finance"
  }
]
```

---

## 对应的 TypeScript 类型定义、前端加载逻辑或行业详情页面模板，完全基于你仓库的 Next.js + React + Tailwind 架构

1. ✅ TypeScript 类型定义  
2. ✅ 前端加载逻辑（从 JSON 加载行业数据）  
3. ✅ 行业详情页面模板（支持动态路由）

所有代码均带有标准化注释头，便于直接复用与维护。

---

## 1️⃣ TypeScript 类型定义

建议保存为：`lib/types/industry.ts`

```ts
/**
 * @file industry.ts
 * @description 行业分类类型定义，用于描述行业结构与展示字段
 * @author YYC³
 * @version 1.0.0
 * @created 2025-10-15
 */

export interface Industry {
  id: string
  name: string
  code: string
  icon: string
  path: string
}
```

---

## 2️⃣ 前端加载逻辑

建议放在：`lib/data/industry-loader.ts`

```ts
/**
 * @file industry-loader.ts
 * @description 加载行业分类 JSON 配置并提供查询方法，用于前端展示与路由跳转支持
 * @author YYC³
 * @version 1.0.0
 * @created 2025-10-15
 */

import type { Industry } from "@/lib/types/industry"
import industriesData from "@/lib/config/industries.json"

export function getAllIndustries(): Industry[] {
  return industriesData
}

export function getIndustryById(id: string): Industry | undefined {
  return industriesData.find((item) => item.id === id)
}
```

> ⚠️ 注意：你需要在 `tsconfig.json` 中启用 `"resolveJsonModule": true` 才能直接导入 JSON 文件。

---

## 3️⃣ 行业详情页面模板（动态路由）

建议放在：`app/industries/[id]/page.tsx`

```tsx
/**
 * @file [id]/page.tsx
 * @description 行业详情页面模板，支持动态路由展示行业信息与图标标识等内容展示组件结构化渲染
 * @author YYC³
 * @version 1.0.0
 * @created 2025-10-15
 */

"use client"

import { notFound } from "next/navigation"
import { getIndustryById } from "@/lib/data/industry-loader"
import type { Industry } from "@/lib/types/industry"

export default function IndustryDetailPage({ params }: { params: { id: string } }) {
  const industry: Industry | undefined = getIndustryById(params.id)

  if (!industry) return notFound()

  return (
    <main className="min-h-screen bg-slate-950 text-white p-8">
      <div className="max-w-4xl mx-auto space-y-6">
        <h1 className="text-3xl font-bold">{industry.name}</h1>
        <p className="text-slate-400">行业代码：{industry.code}</p>
        <div className="text-6xl">{getIndustryIcon(industry.icon)}</div>
        <div className="mt-6 text-sm text-slate-500">路径标识：{industry.path}</div>
      </div>
    </main>
  )
}

function getIndustryIcon(iconName: string): string {
  const iconMap: Record<string, string> = {
    Sprout: "🌱",
    Utensils: "🍽️",
    TrendingUp: "📈",
    Building2: "🏢",
    Users: "👥",
    Heart: "❤️",
    Film: "🎬",
    Factory: "🏭",
    Code: "💻",
    Palette: "🎨",
    GraduationCap: "🎓",
    Zap: "⚡",
    Leaf: "🍃",
    Scale: "⚖️",
    Truck: "🚚",
    Briefcase: "💼",
    Home: "🏠",
    ShoppingCart: "🛒",
    Car: "🚗",
    Plane: "✈️",
    HeartPulse: "💓",
    Cpu: "🖥️",
    Database: "💾",
    DollarSign: "💰"
  }
  return iconMap[iconName] || "🔷"
}
```

---

## ✨ 整体架构说明

- `lib/config/industries.json` → 行业数据源  
- `lib/types/industry.ts` → 类型定义  
- `lib/data/industry-loader.ts` → 加载与查询逻辑  
- `app/industries/[id]/page.tsx` → 动态详情页面  

你可以在首页或仪表盘中使用 `getAllIndustries()` 渲染行业卡片，并跳转到 `/industries/[id]` 查看详情。

---

## 首页行业导航组件，支持点击跳转到这些详情页（可以加上权限控制或动画效果）
