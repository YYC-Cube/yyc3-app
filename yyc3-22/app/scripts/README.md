# /bin/bash /Users/yanyu/未命名文件夹/M4/TRAE-CN/trae_check_install.sh

# === 脚本健康检查头 ===

set -euo pipefail

echo '========================================'
echo '🚀 项目脚本系统 - README文档'
echo '========================================'
echo ''
echo '📋 目录结构说明:'
echo ' /Users/yanyu/ww/app/scripts/'
echo ' ├── start.sh # 全局启动入口（推荐使用）'
echo ' ├── init.sh # 项目初始化脚本'
echo ' ├── check-env.sh # 环境检查脚本'
echo ' ├── compare-changelog.sh # 版本更新日志比较工具'
echo ' ├── deploy-_.sh # 部署相关脚本'
echo ' ├── ecs-init.sh # 阿里云ECS初始化'
echo ' ├── gen-_.sh # 生成相关脚本'
echo ' ├── generate-keys-_.sh # SSH密钥生成脚本'
echo ' ├── multi-machine-coordination.md # 多机协同文档'
echo ' ├── nas-setup.sh # NAS服务器设置'
echo ' ├── setup-ssh-keys.sh # SSH密钥配置'
echo ' ├── ssh_config_example # SSH配置示例'
echo ' ├── sync-config.sh # 配置同步脚本'
echo ' └── version-_.sh # 版本管理脚本'
echo ''
echo '🔧 使用说明:'
echo ' 1. 推荐使用全局启动脚本: ./start.sh'
echo ' 2. 直接运行特定脚本: ./具体脚本名称.sh'
echo ' 3. Windows环境使用PowerShell运行.ps1脚本'
echo ''
echo '📊 功能分类:'
echo ' 🔧 初始化管理: 项目初始化、密钥生成、服务器配置'
echo ' 🌐 SSH配置管理: SSH密钥设置、配置示例、密钥推送'
echo ' 🚀 部署管理: 阿里云部署、多环境部署、Helm部署'
echo ' 📦 发布管理: 版本发布、更新日志、GitLab发布'
echo ' 📄 文档管理: 多机协同文档、配置示例、文档搜索'
echo ' 🔍 环境检查: 运行环境、系统资源、网络连接'
echo ' ⚙️ 工具集: 配置同步、日志比较、版本升级'
echo ' 📊 项目状态: 目录结构、Git状态、依赖列表'
echo ' 🔄 配置同步: 配置同步、配置验证'
echo ''
echo '⚠️ 注意事项:'
echo ' - 所有脚本均已添加健康检查头(set -euo pipefail)'
echo ' - 脚本运行日志保存在项目logs目录'
echo ' - 配置文件统一存放在项目config目录'
echo ' - 请确保有足够的权限运行脚本'
echo ''
echo '✨ 项目专属规则，确保代码质量与业务一致性！ 🌹'
