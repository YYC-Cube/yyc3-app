/**
 * @file 基础UI组件库索引
 * @description 统一导出所有基础UI组件
 * @module components/common
 * @author YYC
 * @version 1.0.0
 * @created 2024-10-15
 */

import { Button } from './Button';
import { Input } from './Input';
import { Card } from './Card';
import { Checkbox } from './Checkbox';
import { IconButton } from './IconButton';

// 命名导出
export { Button, Input, Card, Checkbox, IconButton };

// 默认导出
export default {
  Button,
  Input,
  Card,
  Checkbox,
  IconButton,
};
