# Docker Compose interpolation variables for config/docker-compose.yml
TZ=Asia/Shanghai
DOCKER_PLATFORM=linux/arm64/v8

# Host port mappings
REDIS_DEV_PORT=6381
REDIS_PROD_PORT=6380
API_DEV_PORT=3000
API_PROD_PORT=3001

# Production password for compose substitution
REDIS_PROD_PASSWORD=redis_yyc3

# Usage:
# 1) Copy to config/.env
# 2) Run: docker compose -f config/docker-compose.yml up -d redis-dev api-dev
# 3) Override on-the-fly with shell env if needed
